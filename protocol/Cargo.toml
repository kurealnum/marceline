[package]
name = "protocol"
version.workspace = true
edition.workspace = true

[lib]
name = "marceline_protocol"
path = "src/lib.rs"

[dependencies]
prost = "0.13"
tonic = "0.12"

[build-dependencies]
tonic-build = "0.12"
