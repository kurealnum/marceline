# Pinned deps for the STT worker (EPIC 3.1), from the worker template
# plus the model-specific stack below the line.
grpcio==1.66.2
grpcio-health-checking==1.66.2
protobuf>=5.26.1,<6.0dev

# --- model-specific ---
# numpy is also all the servicer's own tests need; torch/transformers are
# only pulled in by whisper_backend.py, which worker.py imports lazily.
numpy>=1.26,<3
torch==2.4.1
transformers==4.44.2
# Whisper checkpoints on the hub ship as safetensors; accelerate backs
# `low_cpu_mem_usage=True` during load.
accelerate==0.34.2
