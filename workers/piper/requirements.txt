# Pinned deps for the Piper TTS worker (EPIC 5.5), from the worker
# template plus the model-specific stack below the line.
grpcio==1.66.2
grpcio-health-checking==1.66.2
protobuf>=5.26.1,<6.0dev

# --- model-specific ---
# numpy is also all the servicer's own tests need; piper is only pulled in
# by piper_backend.py, which worker.py imports lazily.
numpy>=1.26,<3
piper-tts==1.2.0
